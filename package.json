{
  "name": "playlist-request-prototype",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev:backend": "cd backend && uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "dev:frontend": "cd frontend && npx expo start",
    "dev:frontend:web": "cd frontend && npx expo start --web"
  }
}

